# Production Environment Variables for Render Deployment

# Server Configuration
NODE_ENV=production
PORT=4000

# Database Configuration
# Replace with your MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/password_vault?retryWrites=true&w=majority

# JWT Configuration
# Generate a strong secret key (32+ characters)
JWT_SECRET=your_super_secure_jwt_secret_key_here_min_32_chars

# CORS Configuration (will be set programmatically)
FRONTEND_URL=https://password-vault-frontend.onrender.com