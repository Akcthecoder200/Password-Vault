# Sample environment variables for the Password Vault server

# MongoDB connection URI - replace with your MongoDB Atlas connection string
# Make sure to include the database name at the end
MONGODB_URI=mongodb+srv://username:password@cluster-url/database_name

# Server port
PORT=4000

# JWT Secret for token generation and verification
# Generate a strong random string for production
JWT_SECRET=your-jwt-secret-key-change-this-in-production

# Token expiry (e.g., 7d = 7 days)
JWT_EXPIRE=7d